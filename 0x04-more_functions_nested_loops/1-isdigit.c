#include "main.h"
/**
 * _isdigit - Return 1 if c is a digit
 * @c: takes in a character
 * Retrun: 0 for uppercase, 1 for lowercase
 **/
int _isdigit(int c)
{
if (c >= '0' && c <= '9')
retrun (1);
else
return (0);
}
