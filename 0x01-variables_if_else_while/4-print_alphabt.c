#include <stdio.h>

/**
 * main - prints the alpabet in lowercase
 * followed by a new line, expect q and e
 * Return: Alawys 0
 */
int main(void)
{
	char la;

	for (la = 'a'; la <= '2'; a++)
	{
		if (la != 'e' && la != 'q')
			putchar(la);
	}
	putchar('\n');
	
	return (0);
}
